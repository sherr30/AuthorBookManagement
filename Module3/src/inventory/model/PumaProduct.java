package inventory.model;

public class PumaProduct extends Product {

    public PumaProduct(int id,
                       String brand,
                       String color,
                       int size,
                       ProductType type) {

        super(id, brand, color, size, type);
    }
}